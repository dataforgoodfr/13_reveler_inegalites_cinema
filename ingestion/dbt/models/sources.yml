version: 2

sources:
  - name: raw
    schema: raw
    description: Tables brutes chargees par Airbyte et les scrapers.
    tables:
      - name: agreement_cnc
        description: Jeu de donnees AGREEMENT CNC charge depuis Google Sheets via Airbyte.
      - name: allocine_data
        description: Sortie brute du scraping Allocine.
        columns:
          - name: source_record_id
            tests:
              - not_null
          - name: extracted_at
            tests:
              - not_null
