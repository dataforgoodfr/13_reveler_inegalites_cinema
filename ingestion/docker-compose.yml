services:
  prefect-server:
    image: prefecthq/prefect:3-latest
    cpus: ${PREFECT_SERVER_CPUS:-0.50}
    mem_limit: ${PREFECT_SERVER_MEM_LIMIT:-512m}
    mem_reservation: ${PREFECT_SERVER_MEM_RESERVATION:-256m}
    pids_limit: ${PREFECT_SERVER_PIDS_LIMIT:-256}
    env_file:
      - .env
    environment:
      PREFECT_SERVER_API_HOST: 0.0.0.0
      PREFECT_SERVER_UI_API_URL: http://localhost:${PREFECT_PORT:-4222}/api
    command: prefect server start --no-services --host 0.0.0.0
    ports:
      - "${PREFECT_PORT:-4222}:4200"
    healthcheck:
      test:
        [
          CMD,
          python,
          -c,
          "import urllib.request as u; u.urlopen('http://localhost:4200/api/health', timeout=1)",
        ]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 60s

  prefect-worker:
    build:
      context: ..
      dockerfile: ingestion/prefect/Dockerfile
    cpus: ${PREFECT_WORKER_CPUS:-1.50}
    mem_limit: ${PREFECT_WORKER_MEM_LIMIT:-2g}
    mem_reservation: ${PREFECT_WORKER_MEM_RESERVATION:-768m}
    pids_limit: ${PREFECT_WORKER_PIDS_LIMIT:-512}
    env_file:
      - .env
    depends_on:
      prefect-server:
        condition: service_healthy
      browserless:
        condition: service_started
    environment:
      PREFECT_API_URL: http://prefect-server:4200/api
      DBT_PROFILES_DIR: /app/ingestion/dbt/profiles
      PLAYWRIGHT_WS_ENDPOINT: ${PLAYWRIGHT_WS_ENDPOINT:-ws://browserless:3000}
    entrypoint: [/bin/bash, /app/ingestion/prefect/start_worker.sh]
    volumes:
      - ..:/app

  browserless:
    image: browserless/chrome:latest
    cpus: ${BROWSERLESS_CPUS:-4.00}
    mem_limit: ${BROWSERLESS_MEM_LIMIT:-4g}
    mem_reservation: ${BROWSERLESS_MEM_RESERVATION:-2g}
    pids_limit: ${BROWSERLESS_PIDS_LIMIT:-1024}
    shm_size: ${BROWSERLESS_SHM_SIZE:-2g}
    ports:
      - "${BROWSERLESS_PORT:-3000}:3000"
    environment:
      PREBOOT_CHROME: "true"
      KEEP_ALIVE: "true"
      CONNECTION_TIMEOUT: "600000"
      MAX_CONCURRENT_SESSIONS: "4"
